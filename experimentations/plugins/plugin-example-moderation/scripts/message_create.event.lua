-- This function will be triggered every time the messageCreate event is fired on a guild
-- where this plugin is enabled
-- * `ctx`: the API of the bot (in reality, it can be named however the dev want
-- * `message`: the message received
--
-- More arguments can be passed to the function based on the event (for example, GuildMemberUpdate and MessageEdit)
function event(ctx, message)
    -- ...
end